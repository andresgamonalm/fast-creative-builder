{
  "name": "fast-creative-builder",
  "version": "0.1.0",
  "private": true,
  "description": "Constructor visual para key visuals, gráficas digitales, landing pages y email marketing.",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "npm run dev --workspace=@fcb/servidor",
    "build": "npm run build --workspace=@fcb/nucleo && npm run build --workspace=@fcb/editor",
    "typecheck": "tsc --build --force",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260906.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.129.0"
  }
}
